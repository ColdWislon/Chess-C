# Real file lives at /home/bertrand/.config/chess-dashboard.env (chmod 600)
# and is loaded by chess-dashboard.service via EnvironmentFile=.
# Get a lichess bot token from https://lichess.org/account/oauth/token
# (it starts with the prefix "lip_" followed by a random string).
LICHESS_TOKEN_RPIBOT73=PASTE_YOUR_TOKEN_HERE
