# Build artifacts
chess-engine-c
chess-engine-c-dbg
chess-engine-c.baseline
*.o
*.d
# Generated by Makefile on each build (build SHA header)
src/build_id.h
# Written by tools/bench_compare.sh; consumed by the dashboard
last-bench.json

# Opening books (re-downloadable; see CLAUDE.md)
book.bin
book.bin.bak

# Texel tuning corpus + per-pass snapshot (regenerable via `make corpus`)
quiet-labeled.epd
texel-snapshot.txt
.venv-corpus/

# Python
__pycache__/
*.pyc
venv/
.venv/

# Secrets — should never be in-tree, but defense in depth
.env
*.env
!*.env.example
chess-dashboard.env

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
